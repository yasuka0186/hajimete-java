# AGENTS.md

## 適用範囲

このファイルはリポジトリ全体に適用する。

本プロジェクトは、ポートフォリオ用の架空Java学習サービス「はじめてJava」のLPと無料体験ページを制作するものである。実サービスの運営やJava実行環境の構築は目的に含まない。

## 正本と優先順位

- 現在のユーザー指示を最優先する。
- `hajimete-java-requirements-package.md`を、機能要件・画面仕様・品質基準・受入条件・13日間スケジュールの正本とする。ユーザーから依頼されない限り、この資料自体は変更しない。
- `職務経歴.txt`がある場合は、制作者紹介やポートフォリオ掲載文の事実確認にだけ使う。実装仕様の根拠にはせず、記載のない経験・役割・成果を推測しない。
- READMEと既存コードは現在の実装状態を示す。正本と矛盾する場合は、勝手に仕様を変更せず差分を報告する。
- 実装結果を大きく左右する矛盾だけ質問する。それ以外は正本に沿って合理的に判断し、判断内容を完了報告へ記載する。

## 作業開始時

1. 適用される`AGENTS.md`と、正本の該当章を確認する。初回または全体設計を扱う場合は正本を最後まで確認する。
2. `git status`、既存ファイル、`package.json`のscripts、READMEを確認する。
3. 依頼対象がDay単位なら、対象Dayの作業と完了条件を明示する。
4. 短い作業計画を提示し、実装依頼ならそのまま編集・検証まで進める。診断・レビュー依頼では、明示されない限り修正しない。

## 13日間スケジュールの扱い

- 「Day X」の依頼では、正本のDay Xのチェック項目だけを実施する。
- 後続Dayの機能を先回りして大きく実装しない。現在Dayを成立させる最小限の土台やインターフェースは作成してよい。
- 未完了項目を成功扱いにしない。環境や権限の都合で確認できない項目は、理由と再確認手順を報告する。
- 無関係なリファクタリング、ライブラリ置換、デザイン変更を同じ作業へ混ぜない。

## 技術構成

- HTML5
- SCSS。クラス名はBEMを基本とし、foundation／layout／component／project／utilityの責務を分ける。
- JavaScript（ES Modules）。DOM操作、問題データ、回答判定、ヒント判定、保存、フォーカス管理を分離する。
- Vite
- Vitest
- Playwright
- ESLint、Stylelint、HTML Validator相当の検証
- GitHub Pages

React、Vueなどのフレームワークへ置き換えない。新しい本番依存関係は、既存技術だけで要件を満たせない場合に限り、追加理由と影響を示してユーザーの了承を得る。

## 実装範囲と禁止事項

- LPは`/`、無料体験は`/trial/`の2ページとする。
- GitHub Pagesのプロジェクトサイトを考慮し、内部リンクとアセット参照をベースパス対応にする。先頭`/`のハードコードで公開環境を壊さない。
- 会員登録、ログイン、バックエンド、データベース、管理画面、決済、外部API、アクセス解析、AI回答生成を追加しない。
- Javaコードを実行・コンパイルしない。JavaScriptで入力を判定し、想定出力だけを表示する。
- 月額980円プランは「準備中」とし、申込可能に見えるリンクや処理を作らない。
- フッターに「ポートフォリオ用の架空サービスのデモ」であることを明記する。
- 実在サービスのような誤認、学習成果・就職を保証する表現、職務経歴の誇張を避ける。

## 無料体験の不変条件

- 問題は全3問。「コードの一部」→「1行全体」→「表示内容の変更」の順とする。
- 入力値は前後の空白・改行だけを除去して判定し、内部の表記は模範解答と一致させる。
- 誤答ヒントは、未入力→命令の不足・誤記→大小文字→ダブルクォーテーション→セミコロン→表示文字列→共通ヒントの順に判定する。
- 正解後は解説とコンソール風の想定出力を表示し、自動遷移しない。「次の問題へ」で進む。
- 完了問題は進捗ナビゲーションから開ける。初期状態は閲覧専用で、「再挑戦」後だけ編集可能にする。再挑戦しても全体の完了実績は失わない。
- 判定はボタンと`Ctrl+Enter`／`Command+Enter`の両方に対応する。
- 保存キーは`hajimete-java:trial:v1`とし、現在問題、完了問題、3問の入力内容を`localStorage`へ保存する。
- 壊れたJSONや未知の問題IDは安全に無視または初期化し、操作不能や技術的なエラー文の露出を起こさない。
- リセットは確認後だけ実行し、キャンセル時は状態を変更しない。

## UI・アクセシビリティ

- LPはスマートフォン優先。無料体験はPCで2列、スマートフォンで縦並びにする。
- 360、390、768、1024、1440pxで横スクロールや重大な崩れを起こさない。
- セマンティックHTML、正しい見出し階層、フォームラベル、代替テキストを使用する。
- キーボードのみで主要操作を完了できるようにし、`:focus-visible`を青色の明確な枠で表示する。
- 正誤を色だけで伝えない。判定後は結果欄へフォーカスを移し、`aria-live`等で正誤とヒント・解説の要点を通知する。
- タップ領域は原則44×44px以上、テキストのコントラストはWCAG 2.2 AAを満たす。
- `prefers-reduced-motion: reduce`では不要なフェードや移動を無効化する。
- 200％拡大でも主要機能を操作できるようにする。

## コード品質

- 問題データと表示ロジックを分離し、文言・キー・問題IDは定数化する。
- HTMLへ大きなJavaScriptやCSSを直書きしない。
- 重複、マジックナンバー、不要なグローバル状態を避ける。
- ユーザー向け文言は、完全初心者が理解できる短い日本語にする。専門用語には必要最小限の補足を付ける。
- 不要な`console.log`、仮テキスト、リンク切れ、秘密情報を残さない。
- コメントは「何をしているか」より、制約や判断理由がコードだけでは分からない箇所へ付ける。
- 既存構成が正本の推奨構成と異なる場合、全面的に並べ替えず、既存規約を尊重しつつ責務を分離する。

## 標準コマンド

既存の`package.json`に同等のscriptsがある場合は、それを優先する。Day 1で新規構築する場合は、少なくとも次の操作を再現可能なscriptsとして用意し、READMEへ記載する。

```bash
npm run dev
npm run build
npm run test:unit
npm run test:e2e
npm run lint
npm run lint:styles
npm run validate:html
```

ブラウザやOSが必要で実行できない確認を、別ブラウザの結果で代用したことにしない。

## 検証方針

- JavaScriptの判定・保存処理を変更したら、関連するVitestを実行する。
- 主要導線やDOM操作を変更したら、関連するPlaywrightシナリオを実行する。
- HTML、SCSS、JavaScriptを変更したら、該当lintとビルドを実行する。
- Day 11以降または全体完了時は、正本にある必須単体テストと13件のE2Eシナリオを確認する。
- Day 12以降は、axe等の自動検査、キーボード操作、主要5幅、対応4ブラウザ、公開相当ビルドを確認する。
- Lighthouseは本番ビルド・公開URL・モバイル条件で4カテゴリ各90点以上を目標とする。未計測なら達成と記載しない。
- 手動確認、Safari、VoiceOver、公開URLなど、実際に確認していない項目は「未確認」と明記する。

## Gitと変更の安全性

- ユーザーの既存変更を保持し、関係のない差分を上書き・整形しない。
- `git reset --hard`、無断の`git checkout --`、広範囲な削除を行わない。
- コミット、push、GitHubリポジトリ作成、GitHub Pages公開は、現在の依頼または対象Dayに明記されている場合だけ行う。
- 変更後は`git diff`と`git status`で対象範囲を確認する。

## 完了条件と報告

作業は、対象Dayまたは依頼範囲の受入条件を満たし、関連する検証結果を確認できた時点で完了とする。完了報告は日本語で、次の順に簡潔にまとめる。

1. 完了した項目
2. 未完了・未確認の項目
3. 作成・変更したファイル
4. 実行したコマンド
5. テスト・ビルド・表示確認の結果
6. 次のDayまたは次に行う作業

## Code Review Rules

- 実際のJava実行、有料申込、会員登録が利用可能に見える変更を重大な問題として指摘する。
- GitHub Pagesのベースパスでリンクやアセットが壊れる変更を指摘する。
- 進捗復元、完了実績、見直し、リセットの状態遷移を壊す変更を重点的に確認する。
- キーボード操作、フォーカス移動、読み上げ、動き軽減を後退させる変更を指摘する。
- 受入条件を満たしていないのにREADMEや報告で達成済みとする変更を指摘する。
